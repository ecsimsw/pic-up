# datasource
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://${PICUP_MEMBER_DB_URL}
spring.datasource.username=${PICUP_MEMBER_DB_USERNAME}
spring.datasource.password=${PICUP_MEMBER_DB_PASSWORD}

# encrypt
data.aes.encryption.key=${DATA_AES_ENCRYPTION_KEY}

# redis
spring.redis.host = ${PICUP_COMMON_REDIS_URL}
spring.redis.port = ${PICUP_COMMON_REDIS_PORT}
