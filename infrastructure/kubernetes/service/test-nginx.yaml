apiVersion: v1
kind: Service
metadata:
  name: test-nginx
  namespace: picup
spec:
  selector:
    app: nginx
  ports:
    - protocol: TCP
      port: 30883          # port of service
      targetPort: 80       # to pod